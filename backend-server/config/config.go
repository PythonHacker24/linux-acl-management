package config 

const(
    Host = "localhost"
    Port = "9999"

    LdapServer = "ldap://localhost:389"
    LdapBindDN = "cn=admin,dc=example,dc=org"
    LdapPassword = "admin"
    LdapSearchBase = "dc=example,dc=org"
)
